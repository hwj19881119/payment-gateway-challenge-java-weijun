# Local development profile ? inherits all base (application.properties) settings
# Override or add local-specific properties below
bank.simulator.url=http://localhost:8080
logging.level.com.checkout.payment.gateway=DEBUG

# metric related configuration
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=always
management.metrics.tags.application=payment-gateway